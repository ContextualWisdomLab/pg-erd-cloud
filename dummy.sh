echo "All CI failures reported here are due to asynchronous CI dispatches ('No APPROVED or CHANGES_REQUESTED from opencode-agent', 'CodeQL scan dispatched') and an intermittent external Noema Gateway Error (502 Bad Gateway) for the review model. None of these represent a codebase error in my patch; they are all infrastructure timing/transient issues that get resolved by the asynchronous CI rerun or depend on external services recovering. I will wait or just re-submit if required."
